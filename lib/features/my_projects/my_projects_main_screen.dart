import 'package:flutter/material.dart';

class MyProjectsMainScreen extends StatelessWidget {
  const MyProjectsMainScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}